import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex: 1,
    },
    listData: {
        padding: 2,
        borderBottomWidth: 1,
        borderRadius: 10,
        borderColor: '#DBDBDB',
        marginVertical: '1%',
        flexDirection: 'row',
    },
    textTitle: {
        fontWeight: '700',
    },
    textDainamic: {
        fontWeight: 'bold',
        color: '#2B6EF2'
    },

});

export default styles;